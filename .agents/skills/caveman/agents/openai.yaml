interface:
  display_name: "caveman"
  short_description: "Mandatory lite mode: fewer internal tokens, unchanged evidence and public prose"
  brand_color: "#6B7280"
  default_prompt: "Apply mandatory caveman lite across agents for terse internal AI communication. Keep exact evidence lossless and public text in normal prose; expand for safety, approvals, or ambiguity. Do not change verification scope."
policy:
  allow_implicit_invocation: true
