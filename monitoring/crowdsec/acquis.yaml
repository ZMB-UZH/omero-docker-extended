# CrowdSec acquisition configuration.
# Defines host log sources for threat detection.

# SSH authentication logs (host-level, mounted via /var/log)
filenames:
  - /var/log/auth.log
  - /var/log/syslog
labels:
  type: syslog
