# Reliability

- Require deterministic startup scripts with explicit failure behavior.
- Keep health and readiness checks documented in `docs/reference/service-endpoints.md`.
- Capture recurring incident classes in `docs/troubleshooting/` and link mitigation steps.
