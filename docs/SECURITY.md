# Security

- Keep secrets out of source control; use env files and deployment-specific secret stores.
- Pin container images and dependencies where possible.
- Treat plugin input as untrusted and validate boundary data explicitly.
