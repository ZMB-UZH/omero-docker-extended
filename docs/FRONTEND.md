# Frontend Guidance

This repository primarily extends OMERO.web via Django plugin packages.

- Keep templates and URL wiring plugin-scoped.
- Document user-facing behavior in plugin docs under `docs/plugins/`.
- When changing UI workflows, include a validation plan in `docs/exec-plans/active/`.
