# OMERO.web runtime override file.
#
# Keep OMERO.web logging on the mounted OMERO.web log volume so logs are
# persisted and consistent with supervisord output paths.
#
# This path is validated for write access by /startup/10-web-bootstrap.sh
# before OMERO.web starts.
omero.web.logdir=/opt/omero/web/OMERO.web/var/log
