# Ensure all docker compose commands automatically load installation paths.
# This keeps docker-compose.yml strict (no fallback paths) while avoiding
# manual --env-file usage for every command.
# ------------------------------------------
COMPOSE_ENV_FILES=env/installation_paths.env
